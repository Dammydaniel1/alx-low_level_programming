#include <stdio.h>
/**An alternative way of displaying output
 *Printing with printf()
 */
/** A main is required for c to run*/
int main(void)
{
/** Using printf directly with the text to display*/
	printf("with proper grammar, but the outcome is a piece of art,\n");
/**To return after execution*/
	return(0);
}
